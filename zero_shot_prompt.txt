function toSnakeCase(text) {
    return text
        .replace(/([A-Z])/g, '_$1')
        .replace(/\s+/g, '_')
        .toLowerCase()
        .replace(/^_+/, '')
        .replace(/_+/g, '_');
}

module.exports = toSnakeCase;